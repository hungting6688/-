# 台股分析機器人核心依賴套件
# 修復版本 - 添加 aiohttp 並指定穩定版本

# 核心數據處理套件
pandas>=1.5.3
numpy>=1.24.2

# HTTP 請求套件
requests>=2.28.2
urllib3>=1.26.0

# 異步 HTTP 套件 (修復版本)
aiohttp>=3.8.0,<3.11.0  # 使用穩定版本範圍
async-timeout>=4.0.0

# 排程任務套件
schedule>=1.1.0

# 環境變數管理
python-dotenv>=1.0.0

# 時區處理
pytz>=2023.3

# 圖表繪製套件 (可選)
matplotlib>=3.7.1

# 郵件傳送增強套件
email-validator>=2.0.0

# 錯誤重試機制
tenacity>=8.2.0

# 文字處理增強
jieba>=0.42.1

# 網頁解析 (用於數據獲取)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 基本工具包
setuptools>=65.6.3
python-dateutil>=2.8.2

# 效能監控 (可選)
psutil>=5.9.0

# 測試套件 (開發用)
pytest>=7.3.0
pytest-mock>=3.10.0

# aiohttp 依賴項 (明確指定以避免版本衝突)
multidict>=4.5,<7.0
yarl>=1.0,<2.0
frozenlist>=1.1.1
attrs>=17.3.0
charset-normalizer>=2.0,<4.0

# 如果 aiohttp 安裝失敗的替代方案註釋
# 可以使用以下命令手動安裝：
# pip install --upgrade pip
# pip install --no-cache-dir aiohttp==3.8.6
# 或者使用預編譯的輪子：
# pip install --only-binary=all aiohttp
