# 台股分析機器人核心依賴套件 - 修復版
# 解決 aiohttp 安裝問題，使用穩定版本

# ==================== 核心數據處理 ====================
pandas>=1.5.3,<2.0.0
numpy>=1.24.2,<2.0.0

# ==================== HTTP 請求套件 ====================
requests>=2.28.2,<3.0.0
urllib3>=1.26.0,<2.0.0

# ==================== 異步支援 (修復版本) ====================
# 使用穩定的 aiohttp 版本，避免編譯問題
aiohttp>=3.8.6,<3.9.0
async-timeout>=4.0.0,<5.0.0

# aiohttp 的必要依賴項，使用兼容版本
multidict>=4.5,<7.0
yarl>=1.0,<2.0
frozenlist>=1.1.1,<2.0
attrs>=17.3.0,<24.0
charset-normalizer>=2.0,<4.0

# ==================== 其他核心依賴 ====================
# 排程任務
schedule>=1.1.0,<2.0.0

# 環境變數管理
python-dotenv>=1.0.0,<2.0.0

# 時區處理
pytz>=2023.3

# 郵件處理
email-validator>=2.0.0,<3.0.0

# 重試機制
tenacity>=8.2.0,<9.0.0

# ==================== 技術分析增強 (可選) ====================
# 如果需要高級技術指標，解除註釋以下行：
# TA-Lib>=0.4.25  # 需要額外安裝，可能有編譯問題
# pandas-ta>=0.3.14b  # 純 Python 替代方案

# ==================== 資料視覺化 (可選) ====================
matplotlib>=3.7.1

# ==================== 網頁解析 ====================
beautifulsoup4>=4.12.0
lxml>=4.9.0

# ==================== 基本工具 ====================
setuptools>=65.6.3
python-dateutil>=2.8.2

# ==================== 效能監控 (可選) ====================
psutil>=5.9.0

# ==================== 測試套件 (開發用) ====================
pytest>=7.3.0
pytest-mock>=3.10.0

# ==================== 備用方案說明 ====================
# 如果仍然無法安裝 aiohttp，系統會自動使用同步模式
# 所有功能仍可正常運作，僅性能略微降低
